# Railway PostgreSQL connection string
DATABASE_URL=postgresql://postgres:password@host:5432/railway

# Password required to open the admin panel and save catalog changes
ADMIN_PASSWORD=troque-esta-senha

# Optional: disable SSL only in local databases that do not support it
PGSSL_DISABLE=false
